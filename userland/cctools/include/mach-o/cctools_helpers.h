#ifndef PANTHERA_CCTOOLS_HELPERS_H
#define PANTHERA_CCTOOLS_HELPERS_H

/*
 * Apple's open-source cctools includes this private header from internal SDKs.
 * The ar/ranlib subset staged for Panthera does not use helper declarations
 * from it, so this compatibility header keeps the build source-based.
 */

#endif
