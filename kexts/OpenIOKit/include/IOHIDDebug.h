#ifndef PANTHERA_MINIMAL_IOHIDDEBUG_H
#define PANTHERA_MINIMAL_IOHIDDEBUG_H

#define HIDLog(fmt, ...) do { } while (0)
#define HIDLogError(fmt, ...) do { } while (0)
#define HIDLogDebug(fmt, ...) do { } while (0)
#define HIDLogInfo(fmt, ...) do { } while (0)
#define HIDLogFault(fmt, ...) do { } while (0)

#define HIDServiceLogFault(fmt, ...) do { } while (0)
#define HIDServiceLogError(fmt, ...) do { } while (0)
#define HIDServiceLog(fmt, ...) do { } while (0)
#define HIDServiceLogInfo(fmt, ...) do { } while (0)
#define HIDServiceLogDebug(fmt, ...) do { } while (0)

#endif
