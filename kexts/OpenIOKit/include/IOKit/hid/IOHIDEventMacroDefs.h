/*
 * Panthera compatibility export for newer IOHIDEvent users. The current
 * in-kernel slice already carries the macro bodies it needs from
 * IOHIDEventData.h, so this header only needs to exist to satisfy includes.
 */
#ifndef PANTHERA_IOHID_EVENT_MACRO_DEFS_H
#define PANTHERA_IOHID_EVENT_MACRO_DEFS_H

#endif
