# Panthera Z5 hybrid state layout.
#
# This file is staged only when PANTHERA_STAGE_ZFS_HYBRID=1. launchd invokes
# /sbin/panthera_zfs_hybrid_mount before importing normal boot jobs when this
# config exists.

POOL=pantherazhybrid
DATASETS=var tmp Users Library
